<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/head") %>
  </head>
  <body>
    <%- include("../partials/host-nav") %>

    <main class="min-h-screen flex items-center justify-center bg-gray-100">
      <div class="w-full max-w-md">
        <h3 class="mb-6 text-2xl font-semibold text-center text-gray-800">
          <%= editing ? "Edit" : "Add" %> your home
        </h3>
        <form
          action="/host/<%=editing ?"edit-home":"add-home"%>"
          method="POST"
          class="bg-white shadow-lg rounded-lg p-8"
        >
        <input type="hidden" name="id" value="<%=editing?home._id:""%>" >
          <div class="mb-4">
            <label for="homename" class="block text-gray-700 font-medium mb-2">
              House Name :
            </label>
            <input type="text" required name="homename" id="homename" value="<%= editing
            ? home.homename : "" %>" placeholder="Enter your house name"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg
            focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>
          <div class="mb-6">
            <label for="price" class="block text-gray-700 font-medium mb-2">
              Price :
            </label>
            <input type="number" required name="price" id="price" value="<%= editing ?
            home.price : "" %>" placeholder="How much Rent you want"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg
            focus:outline-none focus:ring-2 focus:ring-blue-500"  />
          </div>
          <div class="mb-6">
            <label for="location" class="block text-gray-700 font-medium mb-2">
              Loaction :
            </label>
            <input type="text" name="location" required id="location" value="<%= editing
            ? home.location : "" %>" placeholder="Enter your location here "
            class="w-full px-4 py-2 border border-gray-300 rounded-lg
            focus:outline-none focus:ring-2 focus:ring-blue-500" />
          
            <div class="mb-6">
            <label for="image" class="block text-gray-700 font-medium mb-2">
              Upload Image:
            </label>
            <input
              type="file"
              accept="image/jpg, image/jpeg, image/png"
              name="image"
              id="image"
             
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
      
            />
           
            </div>

          <div class="mb-6">
            <label for="Rating" class="block text-gray-700 font-medium mb-2">
              Rating :
            </label>
            <input type="number" step="any" name="Rating" id="Rating" required value="<%= editing ?
            home.Rating : "" %>" placeholder="Enter your Rating here "
            class="w-full px-4 py-2 border border-gray-300 rounded-lg
            focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>
          <div class="mb-6">
            <label for="description" class="block text-gray-700 font-medium mb-2">
              Description :
            </label>
            <textarea   name="description" id="description" placeholder="Enter Your home description"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg
            focus:outline-none focus:ring-2 focus:ring-blue-500" ><%=editing?
            home.description : "" %></textarea>
          </div>

          <input type="submit" value="<%= editing ? "Update" : "Add Home" %>"
          class="w-full bg-blue-500 hover:bg-blue-600 text-white py-2 rounded-lg
          transition duration-200" />
        </form>
      </div>
    </main>
  </body>
</html>
