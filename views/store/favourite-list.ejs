<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/head") %>
  </head>
  <body>
    <%- include("../partials/nav") %>
    <main class="mt-8">
      <h3 class="text-center text-red-500 text-3xl font-bold mb-6">
        Here is your Favourite homes: user
      </h3>
      <div
        class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto p-6"
        style="display: flex; gap: 20px"
      >
        <% if (favouriteHome && favouriteHome.length > 0) { %> <%
        favouriteHome.forEach(home => { %>

        <div
          class="bg-white rounded-lg shadow-lg overflow-hidden border border-gray-200 hover:shadow-xl transition-shadow duration-300"
          style="width: 310px; height: auto; padding: 15px"
        >
          <img
            src="<%= home.image ? home.image : 'https://i.pinimg.com/736x/f0/fc/17/f0fc17618d45910665e1efb85c1c0a72.jpg' %>"
            alt="<%= home.homename %>"
            style="width: 310px; height: 200px; object-fit: cover"
            onerror="this.src='/path/to/default-image.jpg';"
          />
          <div class="p-4">
            <h3 class="text-xl font-bold text-gray-800 mb-2">
              <%= home.homename %>
            </h3>
            <p class="text-sm text-gray-600 mb-2"><%= home.location %></p>
            <p
              class="text-lg font-semibold text-gray-700 mb-2"
              style="display: flex; justify-content: space-between"
            >
              <span>Rs <%= home.price %>/night</span>
              <span>Rating: <%= home.Rating %> ⭐</span>
            </p>
          </div>
          <form
            action="/store/favourite-list/delete/<%=home._id%>"
            method="POST"
          >
            <button
              type="submit"
              class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600 transition duration-300"
              style="border-radius: 5px"
            >
              Remove From Favourite
            </button>
          </form>
        </div>
        <% }) %> <% } %>
      </div>
    </main>
  </body>
</html>
